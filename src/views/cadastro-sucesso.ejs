<%- include('partials/head', { titulo: 'Conta criada' }) %>

<main class="auth-wrap">
  <div class="auth-card auth-card-center">
    <div class="auth-icone-ok"><i class="fa-solid fa-circle-check"></i></div>
    <h1>Conta criada!</h1>
    <p class="auth-sub">Bem-vindo(a), <%= nome %>. Sua conta foi criada com sucesso.</p>
    <a href="/login" class="auth-btn auth-btn-link">Ir para o login</a>
  </div>
</main>

<%- include('partials/foot') %>
