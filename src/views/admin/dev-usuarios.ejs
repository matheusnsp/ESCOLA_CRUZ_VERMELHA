<%- include('head', { titulo: 'Usuários Admin' }) %>
<%- include('header', { ativo: 'dev-usuarios' }) %>

<div class="adm-h1-row">
  <h1 class="adm-h1">Usuários Admin</h1>
  <a href="/dev/usuarios/novo" class="adm-btn"><i class="fa-solid fa-plus"></i> Novo admin</a>
</div>

<% if (ok) { %><div class="adm-ok"><i class="fa-solid fa-circle-check"></i> <%= ok %></div><% } %>
<% if (erro) { %><div class="adm-erro"><i class="fa-solid fa-circle-exclamation"></i> <%= erro %></div><% } %>

<div class="adm-box">
  <% if (!linhas.length) { %>
    <p class="adm-vazio">Nenhum usuário admin cadastrado.</p>
  <% } else { %>
    <table class="adm-table" style="vertical-align:middle;">
      <thead>
        <tr>
          <th>Nome</th>
          <th>E-mail</th>
          <th style="white-space:nowrap;">Papel</th>
          <th>Permissões</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% linhas.forEach(function(u) { %>
          <tr style="vertical-align:middle;">
            <td style="vertical-align:middle;"><strong><%= u.nome %></strong></td>
            <td style="vertical-align:middle;"><small class="adm-mut"><%= u.email %></small></td>
            <td style="vertical-align:middle;white-space:nowrap;"><span class="badge ok"><%= u.papel %></span></td>
            <td style="vertical-align:middle;">
              <div style="display:flex;flex-wrap:wrap;gap:4px;">
                <% u.permissoes.forEach(function(p) { %>
                  <code style="font-size:11px;background:#f0f2f5;border-radius:6px;padding:2px 6px;white-space:nowrap;"><%= p %></code>
                <% }) %>
              </div>
            </td>
            <td style="vertical-align:middle;white-space:nowrap;" class="adm-acoes">
              <% if (u.id === meuId) { %>
                <form style="display:flex;gap:6px;align-items:center;flex-wrap:nowrap;">
                  <select name="papel" style="font-size:13px;min-width:130px;" disabled>
                    <% papeis.forEach(function(p) { %>
                      <option value="<%= p %>" <%= p === u.papel ? 'selected' : '' %>><%= p %></option>
                    <% }) %>
                  </select>
                  <button type="button" class="adm-mini" disabled title="Você não pode alterar o próprio papel"><i class="fa-solid fa-check"></i></button>
                </form>
              <% } else { %>
                <form method="POST" action="/dev/usuarios/<%= u.id %>/papel" style="display:flex;gap:6px;align-items:center;flex-wrap:nowrap;">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <select name="papel" style="font-size:13px;min-width:130px;">
                    <% papeis.forEach(function(p) { %>
                      <option value="<%= p %>" <%= p === u.papel ? 'selected' : '' %>><%= p %></option>
                    <% }) %>
                  </select>
                  <button type="submit" class="adm-mini" title="Salvar"><i class="fa-solid fa-check"></i></button>
                </form>
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</div>

<%- include('foot') %>